Coloque aqui os recursos usados pelo electron-builder.

- `icon.ico` : ícone em formato .ico usado pelo instalador Windows.

Observações:
- Gere um arquivo .ico (por exemplo 256x256) e salve como `build/icon.ico`.
- Você também pode colocar outros recursos aqui (ex.: instalador images, license.txt).