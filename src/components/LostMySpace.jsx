// LostMySpace component was removed from the app.
// This stub remains to avoid build/import errors if any references linger.
import React from 'react'

export default function LostMySpace() {
  return null
}
